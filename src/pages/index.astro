---
import Layout from '../layouts/Layout.astro';

const base = import.meta.env.BASE_URL;
const installCommand = 'pip install rastion[tui]';
---

<Layout title="Rastion Hub - Open Source Optimization Lab" description="Open source optimization lab with onboarding-first terminal workflow.">
  <section class="hero" aria-label="Onboarding hero">
    <img class="owl" src={`${base}rastion-logo1-transparent.png`} alt="Rastion owl logo" />
    <p class="catchphrase">The home for all your optimization work - locally.</p>

    <article class="install" aria-label="Install command">
      <p class="install-kicker">Get Started</p>
      <div class="command-row">
        <pre><code data-install-command>{installCommand}</code></pre>
        <button type="button" class="copy-btn" data-copy-install aria-label="Copy install command">Copy</button>
      </div>
      <ul class="install-features">
        <li>Open a terminal-first workflow for optimization projects.</li>
        <li>Run and compare solvers with reproducible benchmark sets.</li>
        <li>Use local tooling with zero cloud lock-in.</li>
      </ul>
    </article>
  </section>

  <script is:inline>
    const copyButton = document.querySelector('[data-copy-install]');
    const commandCode = document.querySelector('[data-install-command]');

    async function copyInstallCommand() {
      if (!(copyButton instanceof HTMLButtonElement) || !(commandCode instanceof HTMLElement)) {
        return;
      }

      const command = commandCode.textContent?.trim();
      if (!command) {
        return;
      }

      try {
        if (navigator.clipboard?.writeText) {
          await navigator.clipboard.writeText(command);
        } else {
          const helper = document.createElement('textarea');
          helper.value = command;
          helper.setAttribute('readonly', '');
          helper.style.position = 'fixed';
          helper.style.opacity = '0';
          document.body.appendChild(helper);
          helper.select();
          document.execCommand('copy');
          document.body.removeChild(helper);
        }

        const originalText = copyButton.textContent;
        copyButton.textContent = 'Copied';
        copyButton.classList.add('is-copied');
        setTimeout(() => {
          copyButton.textContent = originalText || 'Copy';
          copyButton.classList.remove('is-copied');
        }, 1500);
      } catch {
        copyButton.textContent = 'Failed';
        setTimeout(() => {
          copyButton.textContent = 'Copy';
        }, 1500);
      }
    }

    if (copyButton) {
      copyButton.addEventListener('click', copyInstallCommand);
    }
  </script>

  <style>
    .hero {
      width: min(980px, 100%);
      margin: 0 auto;
      display: grid;
      gap: 1.1rem;
      justify-items: center;
      text-align: center;
    }

    .owl {
      width: clamp(8.5rem, 23vw, 14.5rem);
      height: auto;
      filter: drop-shadow(0 16px 24px rgba(0, 0, 0, 0.45));
      animation: owl-float 3.8s ease-in-out infinite;
      pointer-events: none;
    }

    .catchphrase {
      margin: 0;
      font-family: 'Clash Display', sans-serif;
      font-size: clamp(1.8rem, 5.8vw, 3.25rem);
      line-height: 1.08;
      letter-spacing: 0.02em;
      color: var(--text-primary);
    }

    .install {
      width: min(100%, 680px);
      border: 1px solid rgba(167, 129, 255, 0.26);
      border-radius: 18px;
      padding: 1rem;
      background:
        linear-gradient(120deg, rgba(167, 129, 255, 0.16), rgba(255, 207, 92, 0.12) 55%, transparent),
        var(--code-bg);
      box-shadow: 0 16px 34px rgba(0, 0, 0, 0.35);
      text-align: left;
    }

    .install-kicker {
      margin: 0 0 0.42rem;
      color: var(--text-secondary);
      font-size: 0.9rem;
      font-weight: 600;
      letter-spacing: 0.04em;
      text-transform: uppercase;
    }

    .command-row {
      display: flex;
      align-items: stretch;
      gap: 0.55rem;
    }

    pre {
      margin: 0;
      flex: 1;
      border: 1px solid var(--code-border);
      border-radius: 10px;
      overflow-x: auto;
      padding: 0.75rem;
      background: rgba(0, 0, 0, 0.24);
    }

    .copy-btn {
      flex: 0 0 auto;
      min-width: 5.2rem;
      border: 1px solid rgba(255, 255, 255, 0.2);
      border-radius: 10px;
      padding: 0.7rem 0.85rem;
      background: linear-gradient(135deg, var(--coral-bright), #ffd97d);
      color: #1f190f;
      font: inherit;
      font-weight: 700;
      cursor: pointer;
      transition: transform 0.15s ease, filter 0.15s ease;
    }

    .copy-btn:hover {
      transform: translateY(-1px);
      filter: brightness(1.05);
    }

    .copy-btn.is-copied {
      background: linear-gradient(135deg, #66e6b0, #7ef9b5);
    }

    code {
      font-family: 'Courier New', monospace;
      font-size: 0.9rem;
      color: var(--code-text);
    }

    .install-features {
      margin: 0.9rem 0 0;
      padding: 0;
      list-style: none;
      display: grid;
      gap: 0.46rem;
      color: var(--text-secondary);
      font-size: 0.93rem;
    }

    .install-features li {
      display: flex;
      align-items: flex-start;
      gap: 0.5rem;
    }

    .install-features li::before {
      content: '';
      flex: 0 0 auto;
      width: 0.5rem;
      height: 0.5rem;
      margin-top: 0.43rem;
      border-radius: 999px;
      background: linear-gradient(135deg, var(--coral-bright), var(--cyan-bright));
      box-shadow: 0 0 0 2px rgba(255, 255, 255, 0.08);
    }

    @keyframes owl-float {
      0% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-8px);
      }
      100% {
        transform: translateY(0);
      }
    }

    @media (max-width: 640px) {
      .hero {
        gap: 0.8rem;
      }

      .install {
        padding: 0.65rem;
      }

      .command-row {
        flex-direction: column;
      }

      .copy-btn {
        width: 100%;
      }
    }
  </style>
</Layout>
